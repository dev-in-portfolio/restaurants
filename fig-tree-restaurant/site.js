const toggle=document.querySelector('[data-nav-toggle]');const links=document.querySelector('[data-nav-links]');if(toggle&&links){toggle.addEventListener('click',()=>{const open=links.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));});}const search=document.querySelector('[data-menu-search]');const items=[...document.querySelectorAll('[data-menu-item]')];const count=document.querySelector('[data-menu-count]');function filter(){if(!search)return;const q=search.value.toLowerCase();let shown=0;items.forEach(item=>{const ok=item.textContent.toLowerCase().includes(q);item.hidden=!ok;if(ok)shown++;});if(count)count.textContent=`${shown} concept items shown`;}if(search){search.addEventListener('input',filter);filter();}const tool=document.querySelector('[data-tool]');if(tool){const button=tool.querySelector('[data-build-plan]');button?.addEventListener('click',()=>{const guests=tool.querySelector('[name=guests]')?.value||'6';const style=tool.querySelector('[name=style]')?.value||'selected direction';const extra=tool.querySelector('[name=extra]')?.checked;tool.querySelector('[data-tool-output]').innerHTML=`<small>Demo plan</small><h2>${guests} guests • ${style}</h2><p>${extra?'Include the added note and confirm details directly with the restaurant.':'Confirm availability, pricing and operating details directly with the restaurant.'}</p>`;});}const form=document.querySelector('[data-demo-form]');if(form){form.addEventListener('submit',event=>{event.preventDefault();form.querySelector('[data-form-status]').textContent='Demo only — no message was sent.';});}