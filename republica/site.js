const toggle=document.querySelector('[data-nav-toggle]');const nav=document.querySelector('[data-nav]');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));});}const filterButtons=[...document.querySelectorAll('[data-menu-filter]')];const cards=[...document.querySelectorAll('[data-menu-card]')];filterButtons.forEach(button=>button.addEventListener('click',()=>{filterButtons.forEach(b=>b.classList.remove('active'));button.classList.add('active');const value=button.dataset.menuFilter;cards.forEach(card=>card.hidden=value!=='all'&&card.dataset.category!==value);}));const planner=document.querySelector('[data-planner]');if(planner){const kind=planner.querySelector('[data-plan-kind]');const size=planner.querySelector('[data-plan-size]');const output=planner.querySelector('[data-plan-output]');const priority=planner.querySelector('[data-plan-priority]');const title=planner.querySelector('[data-plan-title]');const copy=planner.querySelector('[data-plan-copy]');const update=()=>{output.textContent=`${size.value} guest${size.value==='1'?'':'s'}`;title.textContent=kind.options[kind.selectedIndex].text;copy.textContent=`A ${priority.value.toLowerCase()} path for approximately ${size.value} guest${size.value==='1'?'':'s'}. Final availability, pricing, timing and policies require restaurant confirmation.`;};[kind,size,priority].forEach(el=>el.addEventListener('input',update));update();}document.querySelectorAll('[data-demo-form]').forEach(form=>form.addEventListener('submit',event=>{event.preventDefault();const note=form.querySelector('[data-form-note]');if(note)note.textContent='Preview complete. No information was sent or stored.';}));